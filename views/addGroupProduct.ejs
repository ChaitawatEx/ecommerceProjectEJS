<%- include('header') %> 
<form method="post">
    <div class="box">
        <div class="card2">
            <div class="card-title">เพิ่มรายการประเภทสินค้า</div>
            <div class="mt-3">
                <label>ชื่อ</label>
                <input type="text" name="name" class="form-control" value="<%= groupProducts.name%>">
            </div>
            <div class="mt-3">
                
               <button type="submit" class="btn btn-info"> Save </button>
            </div>
        </div>
    </div>
</form>
<%- include('footer') %> 